<%- include('../partials/header') %>
<h1>Clothing</h1>

<tbody>
  <% clothing.forEach(function(item) { %>
    <tr>
      <td><%= item.brand %></td>
      <td><%= item.productName %></td>
    </tr>
    <% }); %>
  </tbody>

<%- include('../partials/footer') %>
